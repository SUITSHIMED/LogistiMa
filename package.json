{
  "name": "logistima-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "test": "npm test -w backend"
  }
}